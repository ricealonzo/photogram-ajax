// i would like for the like text to read unlike  and the heart icon to be replaced with the fill heart icon after being .
// find the like form with jquery
//find the 
//    
let unlike = $("#<%= dom_id(@like) %>")

$("#<%= dom_id(@like.photo) %>_new_like_form").replaceWith(unlike)
